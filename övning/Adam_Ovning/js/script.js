/* let användare = {
    id: Number,
    namn: String,
    ålder: Number,
    aktiviteter: []
}
function läggTillAnvändare(id, namn, ålder){
    användare.id = id
    användare.namn = namn
    användare.ålder = ålder
}
läggTillAnvändare(69, "Adam", 26)
console.log(användare) */